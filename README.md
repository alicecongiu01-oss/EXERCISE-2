This code implements a bigram character-level model that generates Italian names. The model is trained on a dataset of 9,133 names and produces 50 new names by sampling from the learned probability distribution.

Examples of generated names: ida, rino, vinimitta, findenisammio, alimana.

Requirements: Python, PyTorch, Matplotlib.

The code was developed and tested on Google Colab.
